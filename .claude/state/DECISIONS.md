# DECISIONS.md — 利用者が決めたこと（追記のみ・消さない）

> **このファイルは追記専用**。決まったことをその場で足す。過去の行は書き換えない。
> コンテキスト圧縮で真っ先に失われるのが「なぜこの方針なのか」なので、ここに残す。
>
> 迷ったら**このファイルが正**。会話の記憶や要約より優先する。

書き方: `### YYYY-MM-DD — 見出し` の下に、**利用者の原文**と**決定内容**を並べる。

---

### 2026-09-06 — 全監査（v0.11.0）の進め方

**利用者の原文**

> NTEwikiの機能やシステムを全監査してください。以前は調べることに制限をかけていましたが
> 今はフルアクセス可能なので全てを調べれます。NTEwikiの今までに追加した全てを手動で1つ1つ
> 手を抜かず確認しよりよく改修や新機能追加やバグ不具合修正を行ってください。

`AskUserQuestion` で確定した3点:

| 論点 | 決定 |
| --- | --- |
| 進め方 | **コードの不具合修正と記事の鮮度回復を両方並行**（重要度順に交互に進める） |
| 新機能の方向 | **4方向すべて**（調べものの使い勝手／ツールの追加・強化／データの見せ方／表示速度・軽さ） |
| 記事の更新日 | **確認したら更新**（Web で裏が取れたら、内容が変わらなくても日付を新しくする） |

> ※ 3つ目は実装時に一歩進めた。`updated`（書き換えた日）を動かすと「書き直した」と
> 誤解されるため、`checked`（確認した日）を新設して**2つに分けた**。
> 利用者の意図（＝確認済みだと読者に伝える）はそのまま満たしている。

---

### 2026-09-06 — バージョンとプランの扱い

**利用者の原文**

> v0.11.0として。過去のプランは消さず保存(番号降って) / 新しいファイルでプラン

- 今回の監査は **`beta v0.11.0`** に含める（まだ `main` に出ていないので、
  リリースノートの v0.11.0 項目に追記して育てる）。
- 過去のプランは**消さず**、`/root/.claude/plans/` に番号を振って保存する。
  - `01-v0.10.0-multiwiki-hub-settings.md`
  - `02-v0.11.0-new-ui-settings-compact.md`
  - `03-v0.11.0-full-audit.md`

---

### 2026-09-06 — 作業を止めないこと

**利用者の原文**

> 中断せず続けて
> 作業中に利用制限に達しましたが、現在はリセットされています。中断したところから続けてください。
> リリースノートを忘れずに、一応記憶系や保存系のファイル確認その後中断せず元の作業に戻って

- 区切りごとに報告を挟まず、**手を動かし続ける**。
- **リリースノート（`src/data/releaseNotes.ts`）を毎回更新する**。忘れやすい。
- 記憶系・保存系（このフォルダと `CONTEXT.md` / `docs/`）を更新してから元の作業へ戻る。

---

### 恒常 — 本番反映は利用者の判断

- 本番 <https://claude-nte.vercel.app/> は **`main` からビルドされる**。
  作業ブランチ `claude/claude-nte-audit-E1OnP` へいくらプッシュしても本番には出ない。
- **`main` へ出すか（PR を作るか）は利用者が決める**。勝手にマージしない。

---

### 2026-09-07 — wiki を6つに増やす（v0.12.0）

**利用者の原文**

> aは削除せず新しくアークナイツエンドフィールドと原神、鳴潮、崩壊スターレイルを準備
> ただし原神、鳴潮、崩壊スターレイルは準備中/開発中(リンク取得と準備中のみ表示)として
> アークナイツエンドフィールドのみ正式作成としてください。また今回は2つに分け
> 後半はアークナイツエンドフィールドの情報収集と本格的な記事作成など
> 前半は上記のやつとエンドフィールドのベースwiki作成ね。前半終了後報告し私に確認とってから後半スタート。
> 必ず日本語で。また記憶ファイルや情報保存ファイルなどを随時追加したりしてください

> アークナイツ:エンドフィールドは独自UIで完全再現で今ではなく制作時に調べて作って。
> 原神／鳴潮／崩壊:スターレイルは準備中/開発中と表示でもゲームのUIなどについて
> 調べて確認したりしてこっちも各ゲームオリジナル完全再現UIにしてください
> 今のプランに追加するだけで消さないで

**決定**

| 論点 | 決定 |
| --- | --- |
| αテスト wiki | **削除しない**。そのまま残す |
| アークナイツ：エンドフィールド | **正式作成**（`/endfield/`）。11セクション |
| 原神／鳴潮／崩壊：スターレイル | **準備中／開発中**。リンクが取れて「準備中」と出る |
| 4つの UI | **各ゲームのオリジナルUIを完全再現**。準備中3つも「そのゲームらしい画面」にする |
| UI の調べ方 | **今ではなく制作時に調べる**。観察結果は `docs/UI-RESEARCH.md` へ |
| 作業の分割 | **前半＝枠組み＋エンドフィールドのベース／後半＝情報収集と記事作成** |
| 前半後 | **報告して確認を取ってから後半へ**。自動で進まない |
| 前半の記事 | **0件**（`AskUserQuestion` で確定） |
| エンドフィールドの作り | **α のような完全独立デザイン**（`AskUserQuestion` で確定） |

**「完全再現」の線引き（権利配慮）**

様式（配色・形・余白・組み方・操作感）を**観察して自前の CSS で作り直す**こと。
**素材は持ってこない** — 公式スクリーンショット・ロゴ・アイコン画像・フォントは同梱しない。
既存ルール「公式画像・地図は同梱しない」をそのまま適用する。
各ページに**非公式ファンサイトである旨と権利者名**を明記する。

---

### 2026-09-08 — 下層ページまで調べ直す／NTE の UIモードを作る

**利用者の原文**

> もう1回様々なページを調べて再現して
> 一応NTEもNTEのUI再現して(設定から変更可能)にして

**決定**

| 論点 | 決定 |
| --- | --- |
| 調べる範囲 | トップだけでなく**下層ページ**（キャラクター一覧など）と**配信CSSのチャンク全部**を見る |
| NTE の見た目 | **NTE 公式サイトのUIを再現したモードを追加**する。既存の見た目は消さない |
| 切り替え | **設定から変更可能**にする（設定 → 外観 → UIモード）。既定は従来UIのまま |

- 実装は UIモード `nte`（`src/styles/ui-nte.css`）。`UI_MODES` に登録すれば
  設定パネル・起動スクリプトは自動で追従する（配列を書き写さない作り）。
- 公式サイトに明るい配色が無いため、**このモードだけはテーマの明暗によらず暗い画面**になる。
  設定を黙って無視しないよう、**設定パネルの説明文にその旨を書く**。
- 公式のフォント（LINE Seed JP / bahnschrift）は**同梱しない**。
  手元にある人だけに効くよう font-family の先頭に置き、無ければ既定へ落とす。


---

### 2026-09-08 — 公式サイトと同水準にする（動きの仕組みまで）

**利用者の原文**

> 中断せず続けて様々なページを再確認したりコードを取得して完全再現を目指して
> その後後半開始か聞いて

> そのUIの中には公式hpほど高品質ですごいものも完全再現仕組みまで

**決定**

| 論点 | 決定 |
| --- | --- |
| 再現の範囲 | 色と形だけでなく、**公式の「動き」の仕組みまで移植する** |
| 品質の基準 | **公式サイトと同水準**を目指す。UIのどれかは「すごい」と言える出来にする |
| 調べ方 | 下層ページ（記事・一覧・ワールド）まで開き、**コードを取得**して実測する |

- 公式の keyframes・clip-path・transform は**値をそのまま移植**する。
- ただし**そのまま持ち込めないものは理由を書いて変える**:
  - 常時 `infinite` のグリッチ → **3回で止める**（長時間読む wiki のため）。ホバー中は infinite。
  - `mix-blend-mode: screen` → 明るいテーマだけ `multiply`（screen は明るい地で消えるため）。
- **公式のキャッチコピー・画像・ロゴ・フォントは持ってこない**（従来どおり）。
  数字を出すときは**実データ**にする。飾りの偽の数字は置かない。


---

### 2026-09-08 — 表示形式とアニメーションを足し続ける

**利用者の原文**

> 追加でまだ実装していない表示形式やアニメーションも追加

（同じ指示が2回。1回目で実測記録の未実装分を拾い、2回目で表示形式そのものを新設した）

**決定**

| 論点 | 決定 |
| --- | --- |
| 足す対象 | `docs/UI-RESEARCH.md` に**実測として書いたまま未実装**のものを優先する |
| 新しい表示形式 | エンドフィールドの一覧に **行 / タイル / 詳しく** の3形式を新設 |
| 置き方 | **押しても何も起きない UI は置かない**（記事0件なら切り替えを出さない） |
| 公式と変えるとき | 理由をコードのコメントに書く（例: スクロールバー隠しは本文には広げない） |

- 表示形式は `localStorage` に残し、**描画前に起動スクリプトが当てて**ちらつきを防ぐ。
- 動きはすべて `prefers-reduced-motion` と設定の「モーションを減らす」の**両方**で止める。
- ポインタ追従は**タッチ端末では動かさない**（`hover: hover` を確認する）。


---

### 2026-09-08 — 本格リリースの準備

**利用者の原文**

> 本格リリースのために追加で法的文書1式の追加と内部的な説明(仕組みとか詳しい使い方の解説など)などを削除

**決定**

| 論点 | 決定 |
| --- | --- |
| 法的文書 | **4本**（利用規約・プライバシーポリシー・免責事項・著作権/権利表記）＋目次を `/legal/` に置く |
| 導線 | **6つの wiki すべてのフッター**から辿れるようにする |
| 問い合わせ先 | **GitHub の Issues**（全記事の「このページを編集」と同じ場所。すでに公開している導線に揃える） |
| 削除の範囲 | **公開サイトに出ている内部的な説明**（更新履歴の開発項目・仕組みの解説）。<br>`docs/` と `.claude/state/` の**開発用の記憶ファイルは消さない**（別の指示で維持を決めている） |

**プライバシーポリシーの扱い（重要）**

- 書いてあることは**実装を grep で確かめた事実**。
  アクセス解析・広告・外部CDN は0件、Cookie も localStorage の送信も無い。
- **解析や広告を入れるときは、入れる前にこの文書を直す。**黙って計測を始めない。
  （`docs/RECIPES.md` の「5. 法的文書」にも明記した）


---

### 2026-09-08 — リリースノートの書き分け

**利用者の原文**

> 追加事項:リリースノートはざっくりと内部コードには詳細に記載+過去(今回)の削除された説明などを追加

**決定**

| 置き場所 | 何を書くか |
| --- | --- |
| `src/data/releaseNotes.ts`（公開） | **利用者に見える変化だけ**。1項目 1〜2文・目安150字以内。専門用語を使わない |
| `docs/CHANGELOG-INTERNAL.md`（内部・新設） | 実装・実測値・調べ方・失敗と原因・再発防止。**長くてよい** |
| 各ファイルのコメント | その場所を触る人が要る情報（実測値の根拠・落とし穴） |

- **公開側に書かないもの**: コマンド名・CI・テストの本数・削ったコードの行数・
  クラス名やCSSプロパティ・検査スクリプトの話。
- **過去に削除した説明は消さずに残す**。`docs/CHANGELOG-INTERNAL.md` の
  **付録A**（削除した項目の原文）と**付録B**（短くする前の原文）に保存した。

---

### 2026-09-08 — 公開向けの文章の書き方（再発防止）

**利用者の原文**

> リリースし(公開し)多くの人に見てもらうため改善をしてください。法的文書の正式化や
> いつ公開するかは決まっていません。 できていないことを「近日公開」と書くのは正確ではないので、
> 分かった時点でこのページを書き換えます。という言い方、書き方や思い違いをそのまま出さずに
> 済みました。などです。

> 公開向け文書も以前のようにしないように対策するために1部ファイルに記載

**決定 — 公開ページに「運営の過程」を書かない**

引用された2文は**直すべき悪い例**。どちらも書いた側の事情や制作の過程を読者に語っている。

| 出さない（悪い例） | 出す（直した文） |
| --- | --- |
| いつ公開するかは決まっていません。できていないことを「近日公開」と書くのは正確ではないので、分かった時点でこのページを書き換えます。 | 公開の時期が決まりましたら、このページでお知らせします。 |
| 思い違いをそのまま出さずに済みました。 | （公開側には書かない。内部の変更履歴へ） |
| 現時点で載せられる確かな情報がないため、あえて空にしています。 | 公式の発表など、出典で裏が取れた内容から順に追加していきます。 |
| ※ 公開サイトへの反映には main への取り込みが必要です。 | （公開側には書かない。開発の事情） |

**判定のしかた**（迷ったらこれ）

- 読者にとっての**結果と次の案内**だけを書く。**理由・経緯・反省・判断の過程は書かない**。
- 「〜しないことにしました」「〜と思っていましたが」「〜せずに済みました」「あえて」
  「決まっていません」は、公開ページでは使わない。
- ただし**誠実性のための事実**は残す — 「記事はまだありません」「準備中です」
  「この wiki の内容は検証用のダミーです」は**書く**（隠すと嘘になる）。
  消すのは *過程の説明* であって *不都合な事実* ではない。
- 約束できないこと（「近日公開」「◯月公開予定」）は書かない。

**対策の置き場所**

- 判定表: `docs/RECIPES.md` の「公開ページの文章」節
- 機械の検査: `scripts/check-ui.mjs`（公開ページの禁止表現を検出する）
- 削った原文: `docs/CHANGELOG-INTERNAL.md` の付録

---

### 2026-09-10 — 次期ベースを Base に切り替える

**利用者の原文**

> 第2弾はこれが終わって私に確認とってから
> bate UIにNewを次期ベースから外してbase UIを追加 base UIの参考は添付画像参照。
> 参照画像を隅々まで確認後完全再現(wiki名はあくまで一例です)

**決定**

| 論点 | 決定 |
| --- | --- |
| `new` の位置づけ | **「次期ベース」を外す**。通常の UIモード（ベータ）に戻す |
| 次期ベース | **新設する `base`**（`src/styles/ui-base.css`） |
| `base` の見た目 | **利用者が添付した参考画像（スマホ向けゲームwikiアプリの画面集・3枚）を再現** |
| 参考画像の wiki 名 | **あくまで一例**。画像に出ているゲーム名・アプリ名は再現の対象ではない |
| エンドフィールド第2弾 | **この UI 作業が終わってから、利用者に確認を取って着手**（自動で進めない） |

**参考画像から読み取った作法**（実装の根拠。詳細は `src/styles/ui-base.css` の冒頭）

地は薄い青灰／面は白／**青紫のアクセントとグラデーション**／大きな角丸（カード16〜20px・
押しものはピル）／ごく薄い影／ピル型の検索欄／「見出し＋もっと見る ›」／
左サムネ＋右シェブロンの行／選択中だけ青く塗るチップ／下線のタブ／
淡い色地＋濃い文字のバッジ／**下端から浮いた角丸のタブバー**。

**画像から持ち込まないもの**: キャラ絵・ゲームのロゴ・背景写真（権利の都合）。
バナーは**自前のグラデーションと光の円**で作る。

### 2026-09-11 — Base UI の参考画像を追加（2回目）

**利用者の原文**

> 中断せず続けて
> イメージ画像追加(ロゴやwiki名はイメージ)

**決定**

- 参考画像を**3枚追加**（記事ページ／キャラページ／武器一覧／サイドメニュー／
  スプラッシュ／ホーム／ゲーム詳細／検索結果）。
- **ロゴと wiki 名は「イメージ」**＝再現の対象にしない。**意匠の作法だけ**を拾う。
- 拾って実装したもの: 目次のカード化と番号／基本情報テーブルの行／タグのチップ／
  **サイドメニューを濃紺にする**（明るいテーマでもメニューだけ暗いのが画像の作法）／
  検索結果のカード行／情報の囲み（淡い青＋アイコン）。
- **入れなかったもの**（データが無く、入れると捏造になる）:
  閲覧数・いいね数・保存数、お知らせの未読バッジ、プレミアムプランのカード。

### 2026-09-12 — 参考画像の読み違いを訂正する（3巡目）

**利用者の原文**

> 完全再現を目指して隅々まで完全再現

> しっかり画像を確認してくださいちゃんと見て

> コンテキスト圧縮による情報損失を補填する目的で会話ログとコンテキスト圧縮時に
> 閲覧するファイルなどを確認してください。また記憶ファイルなども閲覧

**何が起きたか**

3巡目の実装（下層ヘッダーのグラデ化・フィルタの四角ボタン化）が画像とずれていた。
原因は、**圧縮で画像そのものが失われたまま、要約に残った文章の記憶だけで作業を続けた**こと。
会話ログ（`~/.claude/projects/<...>.jsonl`）から画像を取り出して見直したところ、
**上の2つの節（09-10・09-11）に書いた読み取りに誤りが2件あった**。

> このファイルは追記専用なので、**上の行は書き換えない**。以下が正。

| # | 誤（上の節に書いてあること） | 正（画像6枚を見直した結果） |
| --- | --- | --- |
| 1 | 下端から**浮いた角丸**のタブバー | **6枚すべてで画面下端に接した白い帯**。角丸なし・浮いていない。上に細い境界線 |
| 2 | ホームだけ白いヘッダー、**下層は濃い青紫グラデ** | **「下層だけグラデ」はどの画像にも無い**。1枚は全ページ一律でグラデ、2枚はホームだけ色つきで下層は白＋中央タイトル、3枚はホームも白 |

**3件目（誤りではないが、適用範囲が広すぎた）**

フィルタの「アイコン上・ラベル下の四角ボタン」は、**アイコンを持つものだけ**の作法。
アイコンの無い絞り込み（「すべて／★5／★4」「すべて／攻略／キャラ／武器／素材」）は
画像でも**ピルのまま**。全部を四角にしたので下部ナビに隠れて崩れた。

**決定**

| 論点 | 決定 |
| --- | --- |
| 下部タブバー | **浮かせない**。画面下端に接する白い帯に戻す |
| ヘッダー | **白い面＋グラデのブランド文字のまま**。3巡目で足した「下層だけグラデ」を取り消す。6枚はヘッダーが白いもの3枚・色つき3枚で割れており、**どちらとも決められない**。決められないものは**足さない**（画像に無い装飾を足さないのと同じ理由） |
| フィルタ | **アイコンを持つものだけ四角ボタン**。持たないものはピルのまま |
| 参考画像 | **圧縮で失われたら会話ログから取り出して見直す**（手順は `docs/FINDINGS.md`） |

### 2026-09-12 — 表示形式をすべて再現する／画像を base UI 限定で許可

**利用者の原文**

> 表示形式や全てを再現して欲しい
> あとゲーム画像やキャラ画像はこのUI限定で使用許可します

> 補足ですがこの画像はベースUI用で他のUIに変更はありませんよ

`AskUserQuestion` への回答:

> 管理者設定?から公式サイトから取得と枠を作ってあとから差し込む(推奨)を切り替え可能にして

**伝えた懸念（記録として残す）**

ゲーム画像・キャラ画像の権利は**運営元**にある。サイトの管理者が許可を出しても
権利者の許諾にはならず、非公式ファンサイトで同梱すると削除要請の対象になり得る、
と伝えたうえで利用者が判断した。**この判断は利用者のもの**。

**決定**

| 論点 | 決定 |
| --- | --- |
| 画像を使う範囲 | **UIモード `base` のときだけ**。他の11モードは今までどおり自前生成の図形 |
| 出どころ | **管理ページ（/admin/）から切り替える**。「置いた画像だけ」（既定）／「公式の画像も使う」 |
| 置き場所 | `public/images/characters/<記事のID>.webp`（avif / png / jpg も可） |
| 同梱するか | **しない**。`.gitignore` で除外し、置いた端末でだけ見える |
| 公式の画像 | 記事の frontmatter `officialImage` に**URL を書いて参照するだけ**（同梱しない） |
| 置いていないとき | **今までどおりの生成アバター**。ビルド時に有無を見るので 404 も出ない |
| 参考画像の扱い | **base UI 専用**。他のUIモードには一切手を入れない |

**表示形式（参考画像の再現）**

| 画像の形 | 実装 |
| --- | --- |
| 4列グリッドのキャラ一覧 | 一覧の表示形式に **タイル** を新設（`nte.listview` に `tile`） |
| 横に流れるカード列 | トップの「注目のエスパー」に `data-hscroll`。**base のときだけ**横スクロール |
| 縦長の立ち絵が上に大きく | `.avatar-block` を**絵があるときだけ** 3:4 にする |
| 左サムネ＋右シェブロンの行 | 既存の「コンパクト」がそのまま当たる（7節で対応済み） |

**入れなかったもの**: 一覧の上の「並び順 ▾」。並び順は設定にあり、置くと二重管理になる。

### 2026-09-12 — 管理ページの刷新／base UI を構成から作り替える

**利用者の原文**

> /admin/を完全刷新バグ不具合排除
> baseUIは今までのUIの構成や作りを完全に刷新したUIにして
> フォント変えて構わないですbaseUIでは

**決定**

| 論点 | 決定 |
| --- | --- |
| `/admin/` | **タブで4つに分ける**（状態 / お知らせ / 表示 / この端末のデータ）。`alert()` をやめ画面内の通知へ |
| base UI | **色を被せるのをやめ、画面の骨組みから作り替える** |
| サイドバー | base では**使わない**。ドロワーと下のタブバーで辿る（参考画像に固定サイドバーが無い） |
| ヘッダー | base では**グローバルナビを外す**。ロゴと丸ボタンだけ（画像と同じ）。パソコンでも三本線を出す |
| 本文 | base では**白いカードの中**に入れる（画像は地の上に文字が直接載る画面が1枚も無い） |
| フォント | base だけ**丸ゴシック**。`ui-rounded` ＋ Hiragino Maru Gothic。**見出しは通常のゴシック**（丸ゴシックは太字を持たない環境が多く、合成されてぼやけるため） |
| 外部フォント | **読み込まない**。すべて端末にあるものだけを使う（プライバシーポリシーのとおり） |

**`/admin/` で直したバグ**

| 症状 | 原因 |
| --- | --- |
| 削除が別のお知らせを消しうる | 表示時の**並び順の番号**で `splice` していた → **id で消す** |
| 同じ日に2件書き出すと id が衝突 | id が日付だけだった → **時刻を足す** |
| 「maintenance にしました」と英語が出る | 内部の値をそのまま表示 → **ラベルを引く** |
| 合言葉の照合が無反応になることがある | `digest()` の失敗を拾っていなかった → `.catch` |
| 同じページで操作が二重に起きる | 画面入れ替えでリスナーが重なる → `data-bound` |

**ついでに見つけた既存の不具合**（管理ページ以外にも効く）

- **閉じた `<details>` の中身が幅を持つ**（`content-visibility`）。wiki 切替が閉じていても
  ページが横に広く測られていた。
- **wiki 切替を開くと右が画面の外に出る**（393px の画面で 505px まで）。
- **`.layout` が `grid-template-columns: 1fr`** のままだった（`.app` は直っていた）。

→ 3つとも `docs/FINDINGS.md` に記録した。

### 2026-09-12 — 参考画像の再現を詰め、パソコンを使える形にする（7巡目）

**利用者の原文**

> あの画像をより徹底的に観察して画像のやつ完全再現して。PCでもしっかり見えて使えるようにして

`AskUserQuestion` で確定した2点:

| 論点 | 決定 |
| --- | --- |
| パソコンの画面構成 | **左に濃紺のメニューを常時表示**（参考画像4枚目のサイドメニューを開いたまま固定する） |
| 記事名の一覧（`Sidebar`・259件） | **記事ページ／一覧ページの本文の中に折りたたんで置く** |

**6巡目で起きていたこと（実測）**

| 症状 | 原因 |
| --- | --- |
| 一覧が実質2列で頭打ち | `.content` が 760px 固定で、`auto-fill` の3列ぶん（752px）が入らなかった |
| 行き先が三本線だけ | サイドバーもヘッダーのナビも畳んだうえ、ドロワーは**セクション一覧ページ止まり** |
| 760〜1099px で脇の列が残る（不具合） | `components.css` は 760px から2列にするのに、base の打ち消しが 1100px からだった |

**決定した形（パソコン）**

- 1000px 以上で `.app` を **280px ＋ 本文** の2列にし、`.drawer` を `grid-area: rail` の
  sticky な柱にする。`.drawer` は `BaseLayout` で `.app` の中にあるので
  **マークアップを変えずに**左列へ入る。濃紺の意匠は19節がそのまま出る。
- 本文の最大幅は 1000px 以上で **880px**、1280px 以上で **1040px**。
  `.grid-cards` は `auto-fill` のままなので列は自然に増える（PC で6列まで）。
- ヘッダーのブランドはパソコンで畳む（柱の頭に出るため、2つ並んで見える）。
- `.sidebar` は 960px 以上で本文の下へ。`<details open={いま見ているセクション}>` なので
  **開くのは1つだけ**。各セクションは白いカードに入れた。

**画像の意匠で、まだ入れていないもの（理由つき）**

| 入れていないもの | 理由 |
| --- | --- |
| 「便利リンク」の2列グリッド | トップの「データベース」が**既に同じ役目の四角ボタン**になっている。重複する |
| 検索欄の右の「絞り込み」ボタン | 畳む/開くの構造変更と JS が要る。件数の行（「全 N 件」）は実装済みで、画像の左半分は出ている |
| 閲覧数・いいね・ブックマーク | **数える仕組みが無い**。置くと嘘になる（4巡目からの判断を継続） |

### 2026-09-13 — 公式の画像を実際に登録する（8巡目）

**利用者の原文**

> 追加で公式の画像を表示してくれない設定変更しても。あとキャラカードや武器カードなど1番下

> 公式画像の登録あなたがやって

**何が起きていたか**

管理ページで「公式の画像も使う」に切り替えても何も出なかった。仕組みは動いていて、
**記事に `officialImage` が1件も書かれていなかった**のが原因。

**決定**

| 論点 | 決定 |
| --- | --- |
| 登録の作業 | **こちらで行う**（利用者の指示）。公式サイトの配信画像の URL を記事に書く |
| どの画像を使うか | 公式サイトのキャラ紹介の**スマホ版ポスター**（`/public/m/images/…/role-poster-*.png`）。人物が中央にあり、**背景が透過**なので属性色の地と馴染む |
| 同梱するか | **しない**（従来どおり）。URL を参照するだけ |
| 一覧の小さい絵 | 全身だと顔が小さいので、**公式の画像のときだけ**顔のあたりへ寄せる（200%・24%） |
| 記事の大きい絵 | 全身のまま（3:4） |

**伝えるべきこと（権利）**

- 参照＝**相手のサーバーから直接読み込む**形。画像の権利は運営元にある。
- 既定は今までどおり「置いた画像だけ」。**切り替えた端末でだけ**絵が出る。
- 全員に見せる場合は `DEFAULT_IMAGE_SOURCE` を変えてビルドし直す判断が要る。

**キャラの対応づけ（18枠 → 記事17件）**

公式サイトの名前ロゴ画像（`role-name-*.png`）と紹介文を1件ずつ読んで突き合わせた。
ファイル名は中国語名・音写で、**記事の ID とは一致しない**。

| 公式 | 記事 | 公式 | 記事 | 公式 | 記事 |
| --- | --- | --- | --- | --- | --- |
| baicang | baicang | fadiya | fadia | hasuoer | hathor |
| jiuyuan | jiuyuan | mint | mint | nanally | nanally |
| xiaozhi | chiz | an | lacrimosa | xun | hotori |
| yi | irohi | ka | chaos | zhen | shinku |
| canhong | zanko | lingke | rinko | dfde | daffodill |
| zaowu | sakiri | zero-male | zero | zero-female | （未使用） |

**根拠にしたもの**: 名前ロゴを読んだもの（イロヒ・カオス・ダフォディール・ちぃちゃん・
レクイエム・リンコ）／紹介文の固有名詞（白蔵・ファルディーヤ・ハソール・九原・ミント・
ナナリ・エイボン店主＝潯・猩紅の鋭刃＝残虹・ドラゴン娘＝真紅・**鬼郎丸＝早霧の武器**）。

### 2026-09-13 — 呼び方をゲーム内に合わせる

**利用者の原文**

> 一部の書き方の修正(例:エスパーなど)ゲーム内と違う呼び方はやめて。
> アークナイツエンドフィールドやその他のゲームにもこの方式を適応

> 一通り日本のwikiで確認

**決定**

| 論点 | 決定 |
| --- | --- |
| 何を正とするか | **日本の攻略wikiで実際に使われている表記**（神ゲー攻略・ゲームエイト・電撃オンライン・AppMedia） |
| 「エスパー」 | **キャラクター**（利用者が選択）。ただし世界観・設定の文では「異能者」 |
| 旧称 | **消さずに用語記事の `aliases` へ残す**（検索で見つかるように） |
| 適用範囲 | **全 wiki**。エンドフィールドは「スカウト」「オペレーター」「属性」が正しく、直す所は無かった |

**直したもの（NTE）**

エスパー→キャラクター／クリティカルドッジ→極限回避／クリティカルリポスト→極限反撃／
スワップ→交代／エスパー能力素材→異能強化素材／アルティメット→EXレール終結。

**あわせて見つかった内容の誤り**

「バイレール＝アルティメット（必殺技）」と書いていたが、実際は**別物**だった。
バイレールスキル＝クールダウン制のスキル、**EXレール終結＝エネルギーを使う必殺技**。
用語記事を書き直し、戦闘ページにスキルの種類の表（コンビスキル・サポートスキルを含む）を足した。

**残っている要確認**: エンドフィールドの「職分（クラス）」は、攻略サイトで「タイプ」「職業」と
割れていて断定できない。併記のまま残した。

### 2026-09-13 — 管理・DB・iOS アプリ・base UI（9巡目）

**利用者の原文**

> 管理システムの徹底改修+データベースの導入準備+swift移行の準備+
> 以前のbaseUIの参考画像の通りに徹底的に画像を確認しbaseUIを改修+
> 既存機能の実用性向上。全体品質の引き上げ

`AskUserQuestion` で確定した2点:

| 論点 | 決定 |
| --- | --- |
| データベース | **いまの段階では Neon を導入しない**。用途は4つとも想定する（記事データの置き場／利用者ごとの保存／閲覧数・いいね／管理ページのデータ共有）ので、**あとから挿せる形に整える所まで**が今回 |
| Swift | **iOS アプリを新しく作る**。今回はその土台（記事データを機械が読める形で書き出す・設計をまとめる） |

**この2つから決まること**

- 記事データを **JSON で書き出す**（アプリからも DB 移行時にも使える土台）。
- 端末に保存している値（設定・お気に入り・お知らせ・サイトの状態）を、
  **保存先を差し替えられる形**に整理する。いまは localStorage、将来は DB。
- **導入していない機能を「ある」と書かない**（プライバシーポリシーを含む）。
  解析・広告・Cookie を入れるときは、入れる前に `/legal/privacy/` を直す（従来どおり）。
