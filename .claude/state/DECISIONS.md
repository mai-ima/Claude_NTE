# DECISIONS.md — 利用者が決めたこと（追記のみ・消さない）

> **このファイルは追記専用**。決まったことをその場で足す。過去の行は書き換えない。
> コンテキスト圧縮で真っ先に失われるのが「なぜこの方針なのか」なので、ここに残す。
>
> 迷ったら**このファイルが正**。会話の記憶や要約より優先する。

書き方: `### YYYY-MM-DD — 見出し` の下に、**利用者の原文**と**決定内容**を並べる。

---

### 2026-09-06 — 全監査（v0.11.0）の進め方

**利用者の原文**

> NTEwikiの機能やシステムを全監査してください。以前は調べることに制限をかけていましたが
> 今はフルアクセス可能なので全てを調べれます。NTEwikiの今までに追加した全てを手動で1つ1つ
> 手を抜かず確認しよりよく改修や新機能追加やバグ不具合修正を行ってください。

`AskUserQuestion` で確定した3点:

| 論点 | 決定 |
| --- | --- |
| 進め方 | **コードの不具合修正と記事の鮮度回復を両方並行**（重要度順に交互に進める） |
| 新機能の方向 | **4方向すべて**（調べものの使い勝手／ツールの追加・強化／データの見せ方／表示速度・軽さ） |
| 記事の更新日 | **確認したら更新**（Web で裏が取れたら、内容が変わらなくても日付を新しくする） |

> ※ 3つ目は実装時に一歩進めた。`updated`（書き換えた日）を動かすと「書き直した」と
> 誤解されるため、`checked`（確認した日）を新設して**2つに分けた**。
> 利用者の意図（＝確認済みだと読者に伝える）はそのまま満たしている。

---

### 2026-09-06 — バージョンとプランの扱い

**利用者の原文**

> v0.11.0として。過去のプランは消さず保存(番号降って) / 新しいファイルでプラン

- 今回の監査は **`beta v0.11.0`** に含める（まだ `main` に出ていないので、
  リリースノートの v0.11.0 項目に追記して育てる）。
- 過去のプランは**消さず**、`/root/.claude/plans/` に番号を振って保存する。
  - `01-v0.10.0-multiwiki-hub-settings.md`
  - `02-v0.11.0-new-ui-settings-compact.md`
  - `03-v0.11.0-full-audit.md`

---

### 2026-09-06 — 作業を止めないこと

**利用者の原文**

> 中断せず続けて
> 作業中に利用制限に達しましたが、現在はリセットされています。中断したところから続けてください。
> リリースノートを忘れずに、一応記憶系や保存系のファイル確認その後中断せず元の作業に戻って

- 区切りごとに報告を挟まず、**手を動かし続ける**。
- **リリースノート（`src/data/releaseNotes.ts`）を毎回更新する**。忘れやすい。
- 記憶系・保存系（このフォルダと `CONTEXT.md` / `docs/`）を更新してから元の作業へ戻る。

---

### 恒常 — 本番反映は利用者の判断

- 本番 <https://claude-nte.vercel.app/> は **`main` からビルドされる**。
  作業ブランチ `claude/claude-nte-audit-E1OnP` へいくらプッシュしても本番には出ない。
- **`main` へ出すか（PR を作るか）は利用者が決める**。勝手にマージしない。
