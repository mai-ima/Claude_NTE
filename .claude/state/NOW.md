# NOW.md — いまどこにいるか

> **区切りごとに丸ごと上書きする**（追記しない）。40行以内に収めること。
> 長くなったら、それは `CONTEXT.md` の「作業ログ」へ送るべき内容。

最終更新: 2026-09-14 / ブランチ `claude/claude-nte-audit-E1OnP` / `ver.bate.0.14.5`

## いま持っている「正」の資料

- **公式wikiのデザイントークンを丸ごと取れた**（`--EHP7_*` 明 / `--ZhXi_*` 暗）。
  色を画面から数える必要はもう無い → `docs/UI-RESEARCH.md` の一覧
- **公式サイトの実ページは3つだけ**（`/`・`/operator`・`/news`）。
  ほかのパスは**トップにフォールバック**する。Next.js なので 404 でも 200 が返る
- 公式サイトは**重いローディング**が入る。`capture-ui.mjs` は **35秒**待つこと
- 同梱画像 **1690枚・62.3MB**。内訳は管理ページの「ギャラリー」で1枚ずつ見られる

## いまの状態

- 記事 1354本／ページ 1423／`pnpm verify` は通る（リンク切れ0・禁止表現0）
- **新base のキャラ画像**: ズレ（顔寄せの二重適用・`block` で顔アイコンを選んでいた）と
  大きさ（240px 上限・絵の比そのまま）を直した
- **管理ページに「ギャラリー」**: 1690枚を16分類・折りたたみ・名前で絞り込み
- **エンドフィールドを公式wikiに寄せた**: 本物のトークン／書体／カードの3層構造／
  「かたまり」の枠（灰色の帯44px＋黄色の飾り＋16px太字）／タイルは細い画面でも2列

## 次の一手

1. **公式サイト×公式wikiの完全再現の続き**（→ 持ち越し）。
   まだなのは **公式wikiの暗いサイドバー（248px・`rgba(18,18,18,.9)`）**と、
   記事ページ側の「かたまり」化（いまは一覧だけ）。
2. 段階6（NTE 側288本のファクトチェック）の本体。キャラ25本を1本ずつ。
3. エンドフィールドのエリア・システム・イベント・ストーリー・ガイドは
   公式wikiに項目が無く1〜2本のまま。攻略サイト頼り。
4. 一時的な撮影スクリプトは `/tmp/shot-keep.mjs` に退避（リポジトリには置かない）。
