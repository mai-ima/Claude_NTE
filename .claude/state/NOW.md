# NOW.md — いまどこにいるか

> **区切りごとに丸ごと上書きする**（追記しない）。40行以内に収めること。

最終更新: 2026-09-14 / ブランチ `claude/claude-nte-audit-E1OnP` / `ver.bate.0.14.5`

## いま持っている「正」の資料

- **公式wikiのデザイントークン**（`--EHP7_*` 明 / `--ZhXi_*` 暗）と
  **公式サイトの部品の入れ子**（`ComponentName_partName__hash` から読める）。
  どちらも `docs/UI-RESEARCH.md` の 2026-09-14 分に一覧がある。色はもう推測しない。
- **公式サイトの実ページは3つだけ**（`/`・`/operator`・`/news`）。
  ほかのパスはトップに戻る。Next.js なので 404 でも 200 が返り、curl では判別できない。
  `capture-ui.mjs` は**35秒**待つこと（重いローディングがある）。
- 同梱画像 1697枚・約62MB。1枚ずつは管理ページの「ギャラリー」で見られる。

## いまの状態（`pnpm verify` 通過・1423ページ・リンク切れ0）

- **タイル表示＝公式サイトのカード**（171×218・絵の上に白い帯・`transform .2s`）
- **見出しの後ろに 279px の中抜き文字**（公式サイトの `HallowText`）
- **左のレールは公式wikiの暗い面**。幅と広がる動きは公式サイトのまま
- **一覧と記事の基本情報は「かたまり」**（灰色の帯44px＋黄色の飾り＋16px太字）
- 新base のキャラ画像のズレ・大きさは修正済み

## 次の一手

1. 段階6（NTE 側288本のファクトチェック）の本体。キャラ25本を1本ずつ。
2. エンドフィールドのエリア・システム・イベント・ストーリー・ガイドは
   公式wikiに項目が無く1〜2本のまま。攻略サイト頼り。
3. まだ写していない公式サイトの意匠 — 右端 207px の黄色い帯（本文を圧迫するので見送り）、
   並び順のセレクト（`Dropdown_trigger` 170×36・地 `rgb(58,58,58)`）。
4. 撮影用の一時スクリプトは `/tmp/shot-keep.mjs`（リポジトリには置かない）。

## 踏んだ罠（忘れやすい）

- **グリッドの子は `position: absolute` にしても、割り当てられたマス目が基準の枠**になる。
  `inset: 0` は「そのマスの中でいっぱい」の意味。`grid-area: auto` で外すと親全体に戻る。
- `import.meta.dirname` は **Vite がまとめると undefined**。`.astro` から読む道は `process.cwd()`。
