# NOW.md — いまどこにいるか

> **区切りごとに丸ごと上書きする**（追記しない）。40行以内に収めること。
> 長くなったら、それは `CONTEXT.md` の「作業ログ」へ送るべき内容。

最終更新: 2026-09-13 / ブランチ `claude/claude-nte-audit-E1OnP`

## いまやっていること

**9巡目（beta v0.14.0）まで完了。利用者へ報告して次の指示を待つ段階。**

| 段 | 内容 | 状態 |
| --- | --- | --- |
| 1〜7巡目 | base UI の再現／骨組みの作り替え／`/admin/` 刷新／パソコン表示 | ✅ |
| 8巡目 | 公式画像を17人ぶん登録。一覧では顔に寄せる | ✅ |
| **呼び方の修正** | エスパー→キャラクター等。**バイレール＝必殺技は誤りだった**ので直した | ✅ |
| **9巡目: 保存と履歴** | 記事の「保存」／`/favorites/`／ホームの「最近見たページ」 | ✅ |
| **9巡目: JSON 書き出し** | `/api/index.json` と `/api/<コレクション>.json` | ✅ |
| **9巡目: 管理ページ** | 「記事」タブ（288本・下書き64・出典なし0）を新設 | ✅ |
| **9巡目: iOS 設計** | `docs/IOS-APP.md` | ✅ |
| **エンドフィールドの記事 第2弾** | 装備・アイテム・エリア・攻略 | ⏸ **利用者の確認待ち** |

## 次の一手

**利用者に報告する。** そのうえで、次のどれに進むかを確認する。

- 参考画像でまだ入れていない意匠（下線タブ／人気順／「こちらも見ています」）は
  **数える仕組みか構造変更が要る**ので保留にしてある（理由は下）。
- エンドフィールドの記事 第2弾は**確認待ち**（2026-09-10 の決定）。自動で進めない。

## 決まりごと（今回ぶん）

- **保存・履歴の読み書きは `src/lib/user-data.ts` の1か所だけ**を通す。
  サーバーを入れるときは `setSource()` に別の実装を渡す。画面は触らない。
- **Neon はまだ入れない**（利用者の決定 2026-09-13）。あとから挿せる形までが今回。
- **数える仕組みが無いものは画面に出さない**（閲覧数・いいね・「こちらも見ています」）。
- 呼び方は**日本の攻略wikiで実際に使われている表記**が正。旧称は `aliases` に残す。

## つまずきやすい所

```bash
pnpm verify        # test → 記事検査 → 型 → build → UI検査 → リンク検査
pnpm test:browser  # 実機相当（build の後。64ページ × 2端末 ＋ 320px 8ページ）
```

- **`pkill` は使わない** ／ **`test:browser` 中に `build` を走らせない**
- **別 wiki へのリンクには `data-astro-reload`**（`check-ui.mjs` が見ている）
- **`display: none` の親の中は、`position: fixed` の子も消える** → `display: contents`
- **この環境のブラウザから外部サイトへは出られない**（公式画像の確認は `context.route()` で差し替え）
- **本番に出るのは `main` だけ**。PR を作るかは**利用者が決める**（未反映 110 コミット）
