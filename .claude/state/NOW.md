# NOW.md — いまどこにいるか

> **区切りごとに丸ごと上書きする**（追記しない）。40行以内に収めること。

最終更新: 2026-09-14 / ブランチ `claude/claude-nte-audit-E1OnP` / `ver.bate.0.14.5`

## 決まり（忘れやすい）

- **絵文字は使わない**（2026-09-14）。記事・画面・内部メモまで全部。
  `★`（レア度）と矢印などの記号は残す。`pnpm test:ui` が検査する。
- 応答は**日本語・敬語**。「宿題」ではなく「持ち越し」と書く。

## いま持っている「正」の資料

- **公式wikiのデザイントークン**（`--EHP7_*` 明 / `--ZhXi_*` 暗）と
  **公式サイトの部品の入れ子**。どちらも `docs/UI-RESEARCH.md` の 2026-09-14 分に一覧。
- **公式サイトの実ページは3つだけ**（`/`・`/operator`・`/news`）。
  Next.js なので 404 でも 200 が返り、curl では判別できない。`capture-ui.mjs` は35秒待つ。
- **公式wikiに任務のデータは無い**（12分類のどれにも入っていない）。攻略サイト頼り。

## いまの状態（記事 1369本・`pnpm verify` 通過）

- エンドフィールドに**任務**の項目（章・サブ・拠点防衛の6本）
- 世界設定の記事（タロスII・星門・アンゲロス・管理人・工業・陣営・種族）とエリア2本
- タイル表示＝公式サイトのカード。見出しの後ろに中抜き文字。レールは公式wikiの暗い面
- 一覧に**並び順のセレクト**（標準・名前・レア度・新しい順）。並べ替えは CSS の `order`
- 自動リンクの誤爆を修正（5757 → 4248本）。表の透けも修正。**絵文字は全廃**

## 次の一手

1. **段階6（NTE 側288本のファクトチェック）**。キャラ25本を1本ずつ攻略サイトと突き合わせる。
   ここが最後に残った大きい持ち越し。
2. エンドフィールドのイベントは2本のまま（公式のお知らせ頼り）。
3. 任務の個別ページは**利用者の指示で作らない**（出典が1サイトに偏るため）。
4. 撮影用の一時スクリプトは `/tmp/shot-keep.mjs`（リポジトリには置かない）。

## 踏んだ罠（忘れやすい）

- **グリッドの子は `position: absolute` にしてもマス目が枠**になる。`grid-area: auto` で外す。
- `import.meta.dirname` は **Vite がまとめると undefined**。`.astro` から読む道は `process.cwd()`。
- 自動リンクは**長い語の一部に短い語が食い込む**。DENY した語も長さの判定には使う。
- rehype を直したら `rm -rf node_modules/.astro node_modules/.vite` してから測る。
