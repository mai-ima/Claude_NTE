# NOW.md — いまどこにいるか

> **区切りごとに丸ごと上書きする**（追記しない）。40行以内に収めること。
> 長くなったら、それは `CONTEXT.md` の「作業ログ」へ送るべき内容。

最終更新: 2026-09-11 / ブランチ `claude/claude-nte-audit-E1OnP`

## いまやっていること

**UIモード「Base」を次期ベースにする（beta v0.14.0）** — 完了。
その前の後半（v0.13.0・エンドフィールドの記事）は第1弾まで完了。

| 段 | 内容 | 状態 |
| --- | --- | --- |
| NTE を Ver.1.3 後半（9/9）に対応 | リンコ実装／軌道外ブレイク／ノスタルジー号80万 | ✅ |
| エンドフィールドの記事 第1弾 | 用語5・システム3・バージョン関連5・武器2・敵1・攻略1（16本） | ✅ |
| **UIモード `base` を新設** | 参考画像（利用者提供）の作法を再現。`new` から次期ベースを外した | ✅ |
| **エンドフィールドの記事 第2弾** | 装備・アイテム・エリア・攻略 | ⏸ **利用者の確認待ち** |
| エンドフィールドに Pagefind 検索 | 記事が数十本になってから | ⬜ |

## 次の一手

**利用者に報告し、第2弾に入ってよいか確認を取る。自動で進まない**
（「第2弾はこれが終わって私に確認とってから」2026-09-10）。

## 記事を書くときの決まり（最重要）

- **スカウトの排出率・天井は書かない**（公式の数値が取れていない）。
- **出典が食い違ったら併記する**（例: 「弓の練磨」／「弓の試練」）。
- **取得できた出典だけを台帳（`docs/ENDFIELD-SOURCES.md`）に書く**。
- 未確認は `status: draft` ＋ 本文に「要確認」。

## つまずきやすい所

```bash
pnpm verify        # test → 記事検査 → 型 → build → UI検査 → リンク検査
pnpm test:browser  # 実機相当（build の後。64ページ × 2端末 ＋ 320px 8ページ）
```

- **`pkill` は使わない** ／ **`test:browser` 中に `build` を走らせない**
- **UIモードで配色を変えるなら `html:root[data-ui=...]`**（`html[...]` では負ける）
- **`.hero` は `.page-head` でもある**（同詳細度だと後ろ勝ち。→ `docs/RECIPES.md`「9.」）
- **下部ナビを浮かせたら `.app` の padding-bottom も増やす**
- イベントの `end` は「◯日 06:59 まで」→ **前日**を入れる
- **本番に出るのは `main` だけ**。PR を作るかは**利用者が決める**
