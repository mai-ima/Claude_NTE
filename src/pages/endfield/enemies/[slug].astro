---
import EndfieldArticle from "../../../components/endfield/EndfieldArticle.astro";
import { detailPaths, renderEntry } from "../../../lib/detail";
import { titleOf } from "../../../lib/content";

export async function getStaticPaths() {
  return detailPaths("endfieldEnemies");
}
const { entry, prev, next } = Astro.props;
const { Content, headings } = await renderEntry(entry);
const d = entry.data as Record<string, any>;

// 冒頭の要点。値があるものだけを拾う（空行を出さない）
const rows = [
  ["分類", d.type],
  ["弱点", d.weakness],
  ["出現場所", d.area],
].filter(([, v]) => v).map(([label, value]) => ({ label, value: String(value) }));

const badges = [d.en, d.reading ? `よみ: ${d.reading}` : null].filter(Boolean) as string[];
---

<EndfieldArticle
  collection="endfieldEnemies"
  entryId={entry.id}
  title={titleOf(entry)}
  description={d.description}
  headings={headings}
  rows={rows}
  badges={badges}
  status={d.status}
  updated={d.updated}
  checked={d.checked}
  sources={d.sources}
  prev={prev ? { href: `/endfield/enemies/${prev.id}/`, title: titleOf(prev) } : null}
  next={next ? { href: `/endfield/enemies/${next.id}/`, title: titleOf(next) } : null}
>
  <Content />
</EndfieldArticle>
