# CONTEXT.md — このリポジトリの記憶ファイル（最重要）

> **このファイルは最重要のコンテキストファイルです。**
> セッション開始時と、コンテキスト圧縮（compact）の直後には**必ずここを最初に読む**こと。
> 圧縮時は、このファイルに加えて**それまでの会話ログ（要約前のやり取り）も確認**し、
> 進行中の指示・保留事項を取りこぼさないこと。
> 会話の言語は**必ず日本語**。

最終更新: 2026-09-08

---

## 1. このリポジトリは何か

**Astro 製の静的サイト**。**複数ゲームの wiki を1サイトに並置**できる構成になっている。

**wiki は6つ**（一覧の正は **[docs/WIKIS.md](./docs/WIKIS.md)**）:

| wiki | URL | 状態 | UI |
| --- | --- | --- | --- |
| **NTE 完全攻略wiki**（既定） | `/` | 運用中 | 共通（BaseLayout） |
| **アークナイツ：エンドフィールド** | `/endfield/` | 運用中（記事0件） | **専用** |
| 原神 | `/genshin/` | 準備中 | **専用** |
| 鳴潮 | `/wuwa/` | 準備中 | **専用** |
| 崩壊：スターレイル | `/hsr/` | 準備中 | **専用** |
| **αテスト（仮）wiki** | `/alpha/` | サンプル（ダミーデータ） | **専用** |

**wiki ごとに UI を完全に分ける**のが方針。各ゲームのUI観察は
[docs/UI-RESEARCH.md](./docs/UI-RESEARCH.md)。

- 公開先: Vercel（静的出力、`base = "/"`）
- リポジトリ: `mai-ima/claude_nte`
- 開発ブランチ: `claude/claude-nte-audit-E1OnP`（作業はここへコミット／プッシュ）

> ### ⚠ 本番に出るのは `main` だけ
> **作業ブランチにいくらプッシュしても本番 URL には反映されない。**
> 本番 <https://claude-nte.vercel.app/> は `main` からビルドされる。作業ブランチは
> Vercel の**プレビューURL**に出るだけ。
>
> 「直したはずなのに直っていない」と感じたら、まずここを疑うこと：
> 1. 見ている URL は本番か、プレビューか
> 2. `git log --oneline origin/main..HEAD` で、本番に未反映のコミットが無いか
> 3. ブラウザの再読み込み（キャッシュされた古い HTML を見ていないか）
>
> 本番へ出すには `main` へのマージが要る（PR を作るかどうかは**利用者の判断**）。

---

## 2. 絶対に守るルール

1. **日本語で応答する**（コード内コメント・記事本文も日本語）。
2. **捏造しない**。記事に書けるのは**出典で裏が取れた事実のみ**。
   - 裏が取れた → frontmatter `status: "verified"`
   - 未検証の数値・仕様を含む → `status: "draft"`（UIに「要確認」バッジ）
   - 断定できない箇所は本文に **要確認** と明記する。
   - すべての記事に `sources`（出典URL）と `updated`（最終更新日）を付ける。
3. **過去バージョンの情報を消さない**。古くなった記述は削除ではなく
   「**いつ時点の情報か**」を添えて残す（→ `src/content/systems/version-history.md`）。
4. **公式画像・地図は同梱しない**（権利配慮）。視覚要素は自前生成のアバターや概略図で代替。
5. **wiki 同士を混ぜない**。コレクション・URL・ナビ・自動リンク辞書・**CSS** まで分離する。
   **wiki ごとに UI を完全に分ける**（専用レイアウト＋専用CSS。トークンも別名で閉じる）。
   ゲームのUIを再現するときは**様式のみ自前で作り直す**。公式の画像・ロゴ・フォントは
   同梱しない。各ページに非公式である旨と権利者名を明記する。
6. ティア評価などの主観情報は「**コミュニティ評価・時点情報・変動あり**」と明記する。

---

## 3. ディレクトリの地図

> **詳細は [docs/](./docs/) にある。コードを読み直す前にそちらを見ること。**
>
> | ファイル | 何が書いてあるか |
> | --- | --- |
> | [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md) | 全ファイルの役割・lib の関数シグネチャ・CSS の読み込み順・localStorage のキー |
> | [docs/RECIPES.md](./docs/RECIPES.md) | 「〜を足すとき触るファイル」の全リスト（設定 / wiki / ツール / 記事 / 静的ページ） |
> | [docs/CHECKS.md](./docs/CHECKS.md) | `pnpm verify` の各検査が禁じていること。落ちたときの直し方 |
> | [docs/FINDINGS.md](./docs/FINDINGS.md) | 読んで確かめた知見と、過去にやった勘違いの「誤 → 正」 |
> | [docs/CHANGELOG-INTERNAL.md](./docs/CHANGELOG-INTERNAL.md) | **開発の詳細な変更履歴**。公開リリースノートは「ざっくり」で、詳細はこちら |
>
> このファイル（CONTEXT.md）は「**今どうなっているか・何をしたか**」に専念する。
>
> **「いま何をしている途中か」は `.claude/state/` にある**（圧縮対策。短く保つ）:
>
> | ファイル | 役割 | 書き方 |
> | --- | --- | --- |
> | [.claude/state/DECISIONS.md](./.claude/state/DECISIONS.md) | 利用者が決めたこと（原文つき）。**迷ったらここが正** | 追記のみ |
> | [.claude/state/NOW.md](./.claude/state/NOW.md) | いまどこか・次の一手 | **丸ごと上書き**・40行以内 |
> | [.claude/state/SESSION-LOG.md](./.claude/state/SESSION-LOG.md) | セッションごとに1〜3行 | 追記のみ |
>
> `.claude/hooks/load-context.sh` が DECISIONS → NOW → CONTEXT の順に自動で流し込む。

```
astro.config.mjs           # サイト設定・旧URLのリダイレクト
src/
  content.config.ts        # 全コレクションのスキーマ（zod）。alpha* も同居
  content/<collection>/    # NTE の記事（characters / terms / systems / …）
  content/alpha-*/         # αテストwiki の記事（alpha-characters / alpha-terms / …）
  lib/
    nav.ts                 # SECTIONS（NTE）/ ALPHA_SECTIONS（α）/ 属性・ロールのメタ
    wikis.ts               # ★マルチwikiの定義（ブランド・ナビ・フッター・アクセント）
    content.ts             # コレクション横断ヘルパ（sections 引数で wiki 切替）
    detail.ts              # 詳細ページ共通ロジック
    rehype-term-links.mjs  # 本文の自動リンク（wiki ごとに別辞書）
  components/              # EntityList / EntityDetail / Sidebar / MobileDrawer …
    alpha/                 # αテスト専用（AlphaList / AlphaArticle）
  layouts/
    BaseLayout.astro       # NTE のレイアウト（wikiId prop を持つ）
    AlphaLayout.astro      # ★αテスト専用レイアウト（NTE の CSS を読み込まない）
  styles/
    alpha.css              # ★αテスト専用のデザインシステム（--a-* トークン）
    ios.css                # ★iPhone 最適化（html[data-ios] のときだけ効く）
  pages/                   # NTE は直下、αテストは pages/alpha/ 配下
  data/releaseNotes.ts     # サイトの更新履歴（リリースノート）
scripts/
  check-links.mjs          # 内部リンク切れ（ビルド後）
  check-content.mjs        # 記事の体裁・誠実性ルール（ビルド不要）
  check-ui.mjs             # ★アイコン参照切れ / wiki跨ぎリンク / ナビ混入（ビルド後）
```

### wiki を1つ増やす手順（4ステップ）

1. `src/lib/nav.ts` に `<GAME>_SECTIONS` を定義（**コレクション名は wiki 間で一意に**）
2. `src/content.config.ts` にコレクションを追加（`src/content/<game>-*/`）
3. `src/lib/wikis.ts` に `WikiMeta` を1件足す
4. `src/pages/<base>/` にページを置く（既存の `pages/alpha/` をコピーするのが早い）

`rehype-term-links.mjs` の `WIKI_GROUPS` にも同じディレクトリを追加する（自動リンク用）。

→ **検査スクリプト側の追記も要る**。抜け漏れのない全手順は
[docs/RECIPES.md](./docs/RECIPES.md#2-wiki-を1つ足す) にある。

---

## 4. 現在の状態（2026-09-08 時点）

- **公開向けの整備**: `/legal/`（規約・ポリシー4本）／`/notices/`（お知らせ）／
  `/admin/`（管理。合言葉で状態の切り替えとお知らせの作成・削除）。
  **管理ページはサーバー認証ではない**（静的サイトのため。操作はその端末の `localStorage` だけ）。
  全員に配る内容は `src/lib/notices.ts` / `src/lib/site-state.ts` にある。
- **サイトのリリース**: `beta v0.12.0`（**wiki 6つ**。一覧の正は [docs/WIKIS.md](./docs/WIKIS.md)）
- **アークナイツ：エンドフィールド**（`/endfield/`）を新設。12セクションの枠だけで**記事は0件**。
  UI は**公式サイトを Playwright で開いて実画面を見て**作った。
  **白地が主**（`#fff` / `#fafafa`）＋ UIパーツの黒 `#191919` ＋ 蛍光イエロー `#fffa00`。
  ※ 黒地はローディング画面だけだった。**後半でエンドフィールドの情報収集と記事作成を行う**（利用者の確認待ち）。
- **原神 / 鳴潮 / 崩壊：スターレイル** は準備中（`kind: 'planned'`）。
  それぞれ**専用レイアウト＋専用CSS**で「そのゲームらしい画面」に「準備中」を出すだけ。
  `noindex` かつ sitemap から除外。**3つとも公式の実測に基づく**
  （原神＝白6px枠と暖色／スターレイル＝右上だけ角丸・青の現在地・金のCTA／
  鳴潮＝黒地に金・極細罫線・左上だけ角丸）。→ [docs/UI-RESEARCH.md](./docs/UI-RESEARCH.md)

- **NTE 本体のバージョン**: Ver.1.3「霧月夜に星還りて」（2026/8/19 配信）に対応済み。
  - 新キャラ: 残虹（呪・DoT・CV井上喜久子）／リンコ（霊・追撃・CV小倉唯・9/9 実装予定）
  - 新エリア: 星暮保護区／本編第6章「霧の巣遊戯」
  - 章番号の逆転（第7章「赤竜討伐譚」が Ver.1.2 で先行、第6章が Ver.1.3）に注意
- **サイトのリリース**: `beta v0.11.0`（記事は全267件、うち draft 53件）
- **記事の鮮度**: 全記事が 2026-09 時点（`updated` か `checked` のどちらかが今月）。
  Ver.1.2／Ver.1.3 の公式告知を全項目突合済み。
- **ページの重さ**: 1ページ約 83KB（サイドバーの無駄を削って 130KB から 36% 減）。
  CSS は約 145KB（UIモード11種ぶん・初回のみ）
- **UIモード**: 11種。うち **`new`（次期ベース候補）だけは画面の骨組みを変える**
  （パソコン＝左の縦レール／スマホ＝浮かぶドック）。他の9種は質感を塗り替えるレイヤー。
  そのうち **`nte`（公式サイト風）だけは配色そのものを差し替える**
  （暗いテーマ＝公式のニュース一覧／明るいテーマ＝公式の記事ページ）。
  見出しのグリッチは**公式の keyframes をそのまま移植**したもので、
  文字の複製は `BaseLayout` 末尾の inline スクリプトが `data-glitch` に写している。
- **αテストwiki**: `/alpha/` に新設（サンプル記事8本）。**NTE とは別UI**（AlphaLayout + alpha.css）
- **wiki 一覧（ハブ）**: `/wikis/` を新設。`WIKI_LIST` から自動生成するので、
  wiki を1つ足してもこのページは触らなくてよい。
- **設定**: 14件（うち多値が6件）。定義は `src/lib/prefs.ts` の一箇所だけ。
  起動スクリプトと「初期化しても残すキー」はそこから自動生成・導出される。

### 既知の注意点・保留

- キャラ名の**読み**は攻略サイト間で揺れる（例: 白蔵＝ばいざん/はくぞう、九原＝じょえん/くはら、
  海月＝みつき/くらげ、翳＝えい/かげ）。本サイトは **GameWith / Game8 系の表記**を採用。
  神ゲー攻略の読みは自動生成らしく不正確なことがあるため、**安易に書き換えない**。
- 未実装キャラ（`implemented: false`）: 明音凛（S・相）／ニーシャ／クロバネ（S・魂）。
- リンコは 9/9 実装予定のため `status: "draft"`。実装後に確定データへ更新すること。
- 恒常S級6体の内訳は Ver.1.1 時点の確認。恒常追加の有無は未確認（要確認のまま）。
- 弧盤の総数「44種」は Ver.1.1 時点。以降は増えるので断定しない。
- **wiki のナビ定義に別 wiki のページを混ぜない**（α のタブから NTE へ飛ぶ事故になる）。
  `test/wikis.test.ts` が検査している。
- **wiki をまたぐリンクには `data-astro-reload` を付ける**。NTE は View Transitions、
  α は独立レイアウトのため、部分入れ替えされると画面が壊れる。`pnpm test:ui` が検査している。
- 日本語本文で `**強調**` の閉じ記号の直前が全角括弧・句読点でも効くのは
  `remark-cjk-friendly` を入れているから。外すと 77 ページ規模で壊れる。

#### 症状から原因を引く

| 症状 | まず疑うところ |
| --- | --- |
| α のタブ／ホームから NTE に飛ぶ | `wikis.ts` の ALPHA ナビに NTE のページが混ざっていないか（`pnpm test`） |
| wiki をまたぐとアイコンが消える | 跨ぐリンクに `data-astro-reload` があるか（`pnpm test:ui`） |
| 本文の `**` が生のまま出る | `remark-cjk-friendly` が `astro.config.mjs` から外れていないか |
| 直したのに本番が変わらない | **`main` にマージされているか**（作業ブランチは本番に出ない） |
| α に NTE の見た目が混ざる | `AlphaLayout` が `alpha.css` 以外を読んでいないか（`pnpm test:ui`） |
| 別 wiki の見た目が混ざる | `check-ui.mjs` の `STYLE_MARKS` に目印クラスを足したか（`pnpm test:ui`） |
| 新しい wiki の検査が素通りする | `check-ui.mjs` の `INDEPENDENT_BASES` に base を足したか |
| 設定パネルのアイコンが空白 | `SettingsPanel.tsx` の `ICON_PATHS` への足し忘れ（検査では出ない） |
| 設定ページが固まる／真っ白 | 描画関数の中でコンポーネントを定義していないか（無限ループ。`docs/FINDINGS.md`） |
| `@media` に書いた CSS が効かない | 同じ詳細度のルールがファイルの**後方**に無いか（`.drawer-toggle` で実際に起きた） |
| UIモードで `--accent` などの色が変わらない | `html[data-ui='x']`（0,1,1）が `themes.css` の `:root[data-theme='…']`（0,2,0）に負けている。**`html:root[data-ui='x']`** と書く |
| 設定が「データ初期化」で消える | `store.ts` の `KEEP_ON_CLEAR`（今は `PREFS` から自動導出） |
| 検査や作業手順が思い出せない | **[docs/](./docs/) を見る**（RECIPES＝手順／CHECKS＝検査／FINDINGS＝知見） |

---

## 5. 作業ログ（新しい順・区切りごとに追記する）

> **運用ルール**: まとまった作業を終えたら、ここに1〜3行で「何をしたか／何が残っているか」を追記する。
> 長い作業の途中でも、区切り（コミット前など）ごとにこのファイルを読み直して方針を確認すること。

### 2026-09-07 — wiki を6つに増やす（beta v0.12.0・前半）

プランは `/root/.claude/plans/05-v0.12.0-multi-wiki-endfield.md`。
**2つに分けた作業の前半**。後半（エンドフィールドの情報収集と記事作成）は利用者の確認待ち。

- **アークナイツ：エンドフィールド**（`/endfield/`）を新設。12セクションの枠のみ、記事0件。
- **原神 / 鳴潮 / 崩壊：スターレイル** を準備中の wiki として追加（各1ページ）。
- **4つとも専用レイアウト＋専用CSS**。共通レイアウトは使っていない。
- **UI は実測して作った**。事前の推測（アンバー `#f0a020`）は外れで、
  公式CSSの実測は黒 `#191919`（85回）× 蛍光イエロー `#fffa00`（57回）だった。
  → 観察の手順と結果は **[docs/UI-RESEARCH.md](./docs/UI-RESEARCH.md)**。
- 基盤の一般化: `kind: 'planned'`、`check-ui.mjs` の α 決め打ちを `INDEPENDENT_BASES` へ、
  wiki ごとの CSS 混線検査を追加。

**残した課題**: エンドフィールドの Pagefind 検索（記事が増えてから）、OG画像。

### 2026-09-12 — 表示形式の再現とキャラ画像の仕組み（4巡目・beta v0.14.0）

利用者の指示「表示形式や全てを再現して欲しい／**ゲーム画像やキャラ画像はこのUI限定で
使用許可します**」。出どころは `AskUserQuestion` で「**管理ページから切り替え可能に**」と確定。

**権利について**: ゲーム画像の権利は運営元にあり、サイト管理者の許可は権利者の許諾に
ならないと伝えたうえで、利用者が判断した。**同梱はしない**作りにしてある。

**画像の仕組み**（`base` UIのときだけ・他の11モードは今までどおり）

- 絵は `<img>` ではなく **`.avatar::before` の `background-image`** で重ねる。
  読めなければ透明のままで、**下の生成アバターが見える**（フォールバックを書かなくていい）。
- `src/lib/asset.ts` を新設し、**ビルド時に `fs.existsSync` で有無を確かめて**
  あるものだけ書き出す。全カードに URL を書くと**キャラの数だけ 404 が飛ぶ**ため。
- `public/images/characters/` は **`.gitignore` で除外**（README だけ追跡）。
  置いた端末でしか見えず、リポジトリにも本番にも入らない。
- 公式画像は frontmatter `officialImage` に **URL を書いて参照するだけ**。
- 切り替えは管理ページ（`/admin/`）。`nte.site.images` に入る。

**表示形式**

| 参考画像の形 | 実装 | 効く範囲 |
| --- | --- | --- |
| 4列グリッドのキャラ一覧 | `nte.listview` に **`tile`** を新設 | 全UIモード |
| 横に流れるカード列 | 「注目のエスパー」に `data-hscroll` | base のみ |
| 縦長の立ち絵 | `.avatar-block.has-img` を 3:4 に | base のみ |

**踏んだこと**: タイルは**名前を1行で切らないと高さが揃わない**（長い名前が2行になる）。
名前の大きさは呼び出し側がインライン style で書いているので `!important` が要った。

**入れなかったもの**: 一覧の上の「並び順 ▾」。並び順は設定にあり、二重管理になる。

### 2026-09-12 — 参考画像の読み違いを戻す（3巡目・beta v0.14.0）

利用者の指摘「完全再現を目指して隅々まで完全再現」→「**しっかり画像を確認してくださいちゃんと見て**」。

**原因**: コンテキスト圧縮で**画像そのものが消えた**あと、要約に残った文章だけを根拠に
3巡目の実装を進めた。**文章の側の誤りに気づけないまま**、誤りの上に新しいルールを積んだ。

**直し方**: 会話ログ（jsonl）に**画像が base64 で残っている**ので取り出して見直した
（手順は `docs/FINDINGS.md`）。実体は**6枚**（3回目の添付は2回目と同一）だった。

**見つかった読み違い2件**

| 誤 | 正 |
| --- | --- |
| 下部ナビは**浮いていて角が丸い** | **6枚すべてで下端に接している。角丸なし** |
| ホームだけ白、**下層は濃い青紫のヘッダー** | **「下層だけ色を変える」画像は1枚も無い**（白3枚・色つき3枚で割れている） |

**3件目**: 「アイコン上・ラベル下の四角いボタン」は**アイコンを持つ絞り込みだけ**の作法。
**NTE の一覧フィルタには SVG アイコンが1つも無い**のに全チップを縦積みにしたので、
背が高くなって下部ナビに隠れ、「すべて」の字が切れていた。

→ 浮くタブバー・下層ヘッダーのグラデ・フィルタの四角ボタン化を**すべて戻した**。
`BaseLayout` に入れた `data-page` も取り消した（base だけのために全モード共通の
マークアップを増やすことになるため）。**参考画像は base UI 専用で、他の11モードは触らない**。

**記憶ファイルの補填**: `SESSION-LOG.md` が 09-07 で止まっていたので 09-08 以降を遡って
追記。`DECISIONS.md` に訂正を追記（追記専用なので上の行は書き換えていない）。

### 2026-09-11 — UIモード「Base」を次期ベースにする（beta v0.14.0）

利用者の指示（原文は `.claude/state/DECISIONS.md` 2026-09-10）。

- **`new` から「次期ベース」を外し**、通常のUIモード（ベータ）に戻した。
- **`base` を新設**（`src/styles/ui-base.css`・約490行）。利用者が添付した
  参考画像（スマホ向けゲームwikiアプリの画面集3枚）の作法を再現したもの。
  読み取った11項目は CSS の冒頭に根拠として残してある。
- 要点: 薄い青灰の地／白い面／青紫のアクセントとグラデーション／大きな角丸／
  ごく薄い影／ピル型の検索欄／「見出し＋もっと見る ›」／右端のシェブロン／
  選択中だけ塗るチップ／淡い色地のバッジ／**下端から浮いた角丸のタブバー**。
- **画像そのもの（キャラ絵・ロゴ・写真）は持ち込んでいない**。バナーは自前の
  グラデーションと光の円で作った。

**踏んだこと**: `.hero` が `.page-head` でもあるため、バナーを濃色にしたら
小見出しが青紫のまま埋もれた（同詳細度で後ろ勝ち）。`.hero.page-head` と重ねて解決。
浮かせたタブバーのぶん `.app` の下余白も増やした。
最初に足していた「見出しの左の縦棒」は**参考画像に無い**ので削除した。

**参考画像の2巡目（同日・3枚追加）**

「イメージ画像追加(ロゴやwiki名はイメージ)」。ロゴと wiki 名は再現対象外という条件で、
記事ページ／キャラページ／武器一覧／サイドメニュー／検索結果の作法を追加で拾った。

- 目次を**カード化し番号を振る**（第2階層だけ）
- 基本情報テーブルの行、タイトル下のタグを淡い青のチップに
- **サイドメニューを濃紺に**（明るいテーマでもメニューだけ暗いのが画像の作法）
- 検索結果をカード行に、「ゲーム情報」の囲みを淡い青に

**入れなかったもの**: 閲覧数・いいね数・保存数、お知らせの未読バッジ、
プレミアムプランのカード。**こちらにデータが無く、置くと嘘になる**ため。

**残した課題**: エンドフィールドの記事 第2弾（**利用者の確認待ち**）。

### 2026-09-09 — 後半開始: エンドフィールドの記事作成＋NTE 全情報更新（beta v0.13.0）

利用者の指示「後半開始+NTE全情報更新」。プランは
`/root/.claude/plans/06-v0.13.0-endfield-articles.md`。

**NTE（Ver.1.3 後半・2026/9/9 実装）**

- [リンコ] を実装確定に（さだちゃんの CV 村瀬迪与を追記）。[潯]の復刻ボード名
  「独り酔い朧月流」、弧盤「万波特別号」の期間を確定。
- **[ノスタルジー号] の価格が 800,000ファンス と確定**（draft → verified）。
- 新規: [軌道外ブレイク]（サバイバー系の新モード）／[ピクセルオーバーフロー]／
  [潮風ツーリング]。[ファンスラッシュ]は v1.3 の回を足し、**過去回は表で残した**。
- バージョン履歴と Ver.1.3 の記事に「後半」の節を追加。

**エンドフィールド（記事0件 → 12本）**

- **出典台帳 `docs/ENDFIELD-SOURCES.md` を新設**してから書いた。
  A（一次）/B（準一次）/C（二次）で確認レベルを記録する。
- 第1弾: 用語（オペレーター・職分・属性）、システム（戦闘・スカウト・集成工業）、
  バージョン「雪氷の幽夢」、特別スカウト「冬狩」、ティフォロス、プチエナ、
  雪松林、冬の夢は霧の中に、武器2本。
- **スカウトの排出率と天井は書いていない**（公式の数値が取れていないため）。
- **出典が食い違う点はそのまま併記した**（上級者イベントの名称が
  PR TIMES「弓の練磨」・gamebiz「弓の試練」で一致しない）。

**踏んだこと**

- 台帳に最初、**取得していない公式ニュースを出典として書いてしまった**。
  取得を試みて失敗したため、台帳を訂正し「公式サイトは JS 描画で取れない。
  現状の一次は PR TIMES のみ」と明記した。
- 記事が入って初めて、一覧の「行」表示で**説明文が省略されず縦に伸びる**と分かった
  （`.ef-row-desc` に line-clamp が無かった）。2行で切るようにした。

**残した課題**: 敵・アイテム・攻略の記事、エンドフィールドへの Pagefind 検索の追加。

### 2026-09-08 — スマホUIの改善とリファクタリング（beta v0.12.0）

- **320px 幅（iPhone SE）の横スクロールを解消**。原因は `.app` の
  `grid-template-columns: 1fr`（`1fr` の最小は中身の min-content）。
  → `minmax(0, 1fr)`。ブランド名も省略できるようにした。
  再発防止に `audit-browser.mjs` へ **iPhone SE で代表8ページ**を巡回する段を追加。
- **`.callout` に段落を直に並べると段落ごとに列になる**（flex の子になるため）。
  管理ページが1文字ずつ折り返していた。本文をまとめ、アイコンの無い callout は
  縦積みにする CSS を入れた。
- 狭い画面向けに、案内のボタン（44px・2列）、状態の選択（1列）、
  お知らせの行（縦積み）を調整。
- **リファクタリング**: フッターの共通リンクを `src/lib/nav.ts` の
  `COMMON_FOOTER_LINKS` に集約（6レイアウトに1か所で反映）、
  準備中3 wiki の `<head>` を `PlannedHead.astro` に集約、
  localStorage のキー名を `site-state.ts` に一元化（inline スクリプトへは
  `define:vars` で渡す）。未使用 import も削除。

**残した課題**: 表示形式・アニメーションの追加（4巡目）。

### 2026-09-08 — 公開向けの文章の直し／お知らせ／管理ページ（beta v0.12.0）

- **公開ページから「運営の過程」を削除**。準備中3ページの「いつ公開するかは決まっていません…」、
  一覧の「あえて空にしています」、リリースノートの「main への取り込みが必要です」など。
  **再発防止を3段構えで入れた** — `DECISIONS.md` の判定表／`docs/RECIPES.md`「6.」／
  `scripts/check-ui.mjs` の禁止表現検査（`pnpm test:ui` で落ちる）。
  **消すのは過程の説明だけ**。「記事はまだありません」等の事実は残す。
- **お知らせ（`/notices/`）を新設**。一覧と個別ページ、フッターからの導線。
- **管理ページ（`/admin/`）を新設**。合言葉でロックを外すと、サイトの状態
  （通常／メンテナンス／停止）の切り替えと、お知らせの作成・削除ができる。
  静的サイトなのでサーバー認証はできず、操作は端末の `localStorage` にしか残らない。
  **その限界を画面に明記**し、全員に配るための貼り付け用の書き出しを付けた。
- 案内を重ねる `SiteStateGate` を6レイアウトすべてに設置。
  `/admin/` `/legal/` `/notices/` では出さない（停止中に閉じ込めを作らないため）。

**残した課題**: スマホUIの改善、全体のリファクタリング、表示形式・アニメーションの追加（4巡目）。

### 2026-09-08 — リリースノートの書き分けと、表示形式・スクロール連動（beta v0.12.0）

利用者の指示「リリースノートはざっくりと内部コードには詳細に記載＋過去（今回）の
削除された説明などを追加」／「追加でまだ実装していない表示形式やアニメーションも追加」。

- **`docs/CHANGELOG-INTERNAL.md` を新設**。開発の詳細（実測値・調べ方・失敗と原因・
  再発防止）はここに書く。**削除した項目と、短くする前の原文を付録に残した**ので
  内容は失われていない。
- **公開リリースノートは「ざっくり」に**。v0.12.0 を 24項目 → 12項目、
  最長 294字 → 130字に。書き分けの決まりは `src/data/releaseNotes.ts` の冒頭コメントに明記。
- **一覧に「表」形式を追加**（`nte.listview` に `table`）。名前・属性/ロール・日付が
  列でそろう。狭い画面では列を崩す（横スクロールを作らない）。
- **目次のスクロール追従**。いま読んでいる見出しを目次で示す。
  印は色だけでなく左の棒でも分かるようにした。
- **読書の進み具合**。`animation-timeline: scroll()` だけで作るので **JS を使わない**。
  非対応ブラウザとモーション抑制時は出ない。

### 2026-09-08 — 本格リリースの準備: 法的文書1式と、内部的な説明の削除（beta v0.12.0）

利用者の指示「本格リリースのために追加で法的文書1式の追加と内部的な説明
（仕組みとか詳しい使い方の解説など）などを削除」。

**法的文書を4本追加**（`/legal/`）:

- 利用規約／プライバシーポリシー／免責事項／著作権・権利表記＋目次ページ
- **6つの wiki すべてのフッター**から `/legal/` へ導線を張った
- 権利表記の表は `WIKI_LIST` から生成するので、**wiki を足せば自動で増える**
- 問い合わせ先は GitHub の Issues（全記事の「このページを編集」と同じ場所に揃えた）

⚠ **プライバシーポリシーは実装の事実に基づいて書いてある**。
書く前に `grep` で確かめた結果:
アクセス解析・広告タグ・外部CDN は**0件**、Cookie を書き出すコードは**無い**、
`localStorage` の内容を送信するコードも**無い**。
**解析や広告を入れるときは、入れる前にこの文書を直すこと**（`docs/RECIPES.md` に明記）。

**内部的な説明を削除**:

- リリースノートから **`【内部の改善】` の5項目**と、`pnpm` コマンド・GitHub Actions・
  Vitest・ビルド成果物といった開発の話に触れた項目を削除（計13項目）
- 消しすぎた2件（共有カード・Liquid Glass）は**読者向けの言い方に書き直して戻した**
- 実装の原因に踏み込んだ表現（「CSSの指定が打ち消されていた」など）を平易化
- 見出し5本から開発用語（拡張性/整備性・テスト基盤・自動化 など）を外した
- `/wikis/` のリード文から内部構造の説明を削除
- エンドフィールドの導入ブロックから、バーの計算方法の説明を削除

### 2026-09-08（深夜） — 表示形式を追加し、残っていた動きを入れる（beta v0.12.0）

利用者の指示「追加でまだ実装していない表示形式やアニメーションも追加」（2回目）。

- **エンドフィールドの一覧に表示形式を新設**（`html[data-ef-view]`）:
  `rows`（既定・行）／`grid`（タイル）／`detail`（説明を全文）。
  選んだ形式は `localStorage['endfield.view']` に残し、**描画前に起動スクリプトが当てる**。
  NTE 側の `data-listview` とは**別の仕組み**（専用レイアウトで設定パネルを読まないため）。
  **記事0件のときは切り替えを出さない**（押しても何も起きない UI を置かない）。
- **折りたたみ `.ef-fold`**（実測のドロップダウン）。矢印が **90度 → 270度** に回る 0.2秒の開閉。
  トップの「このサイトのほかの wiki」に使った。
- 作っただけで使っていなかった部品を**実際に使った**:
  点線のドット（導入ブロックの黒地に重ねる）／下方向のフェード（一覧の下端）。
- **スクロールバーを隠す**（実測）。ただし公式のようにサイト全体ではなく、
  **横に流れる帯とレールだけ**に絞った（本文まで隠すと読み進める手がかりが消えるため）。
- **鳴潮**: 実測の「極細の枠だけの丸い矢印ボタン」＋「左上だけ角丸の戻る」を
  「wiki 一覧へ」に適用。
- **NTE モード**: 公式の `mouseTracking260603.js` に倣い、
  **ヒーローの光がポインタに追従**する。座標は `--nte-mx` / `--nte-my` に入れ、
  JS が動かない環境では既定値のまま。モーション設定とタッチ端末では動かさない。

### 2026-09-08（夜・続き） — 記録だけして未実装だった意匠を全部拾う（beta v0.12.0）

利用者の指示「追加でまだ実装していない表示形式やアニメーションも追加」。

- **エンドフィールド**: シェブロンの並び／点線のドット／下方向のフェード／
  左右端のマスク／計器風の数値（`● 01 / 12`）／**ピル型ページャ＋二重リングの丸ボタン**
  （一覧の下端に前後のセクション導線として実装）／レール下端の黄色いCTA。
- **鳴潮**: 実測の「幅0から伸びる金の下線」（3か所で同じ作法だった）を主要リンクに。
- **原神**: カードが浮く／金の額装が四隅から現れる／四芒星がゆっくり回る。
- **スターレイル**: 触れると**右上の角丸が大きくなる**（統一処理を動きで見せる）。
- **NTE モード**: 一覧の反転を `background-size: 0% → 100%` で**左から伸びる面**にした。

**踏んだ事故（重要）**: CSS のコメントの中に公式CSSを引用したとき、
引用に含まれる閉じ記号でコメントが終わり、**続く `.ef-round` のルールが丸ごと捨てられた**。
症状は「`inset:0` の擬似要素がビューポート大の巨大な円になる」。ビルドもリンタも通る。
→ `scripts/check-ui.mjs` に**コメントの入れ子を見つける検査**を追加した。

**断念したもの**: レール下端のCTAの**縦書き**。`writing-mode: vertical-rl` は
縦組みメトリクスを持たないフォントだと**字が重なって読めなくなる**（この環境で再現）。
`getClientRects()` は正常値を返すので数値では気づけず、高解像度で撮って目で見て分かった。
環境依存で壊れるものは意匠として正しくても採らない、と判断して横組みの短いラベルにした。

### 2026-09-08（夜） — 動きの仕組みまで移植する（beta v0.12.0）

利用者の指示「そのUIの中には公式hpほど高品質ですごいものも完全再現仕組みまで」。
色と形は揃ったので、**公式の「動き」をそのまま動かす**段に入った。

- **NTE のグリッチを本物にした**。公式（`.glitching`）は見出しを3枚重ねて
  `clip-path: inset()` を 0.3s で切り替えている。**keyframes の値は一字一句そのまま移植**。
  - この効果は**文字の複製が要る**（切ると元の文字が欠けるため）。
    公式はマークアップに `data-text` を持つが、こちらは `.page-head h1` を持つページが
    **14種類**あり手書きでは足し忘れる。→ **`BaseLayout` 末尾の inline スクリプトで
    機械的に `data-glitch` へ写す**方式に一本化（`data-astro-rerun` で切替後も走る）。
    設定パネルでの切り替え用に `applyUI()` からも同じことをする。
  - **公式と変えたのは2点だけ**: 回数を `infinite` → **3回**（ホバー中だけ infinite に戻る。
    長時間読む wiki で常時点滅させないため）、明るいテーマだけ `screen` → `multiply`
    （`screen` は明るい地で白く飛んで消えるため）。
  - `prefers-reduced-motion` と設定の「モーションを減らす」の**両方**で止まる。
- **エンドフィールドのトップに導入ブロック**（`.ef-intro`）を新設。
  公式のローディング画面（黒地＋等高線＋左端の黄色い縦バー＋大きな黄色の数字）を再現。
  等高線は `repeating-radial-gradient` 2系統で作る（画像は使わない）。
  バーは表示時に 1.1秒かけて下から伸びる。
  **数字とバーの割合は実データ**（記事が1本以上あるセクション ÷ 全セクション）。
  公式のキャッチコピーそのものは載せず、英字ラベルは自前の言葉にしてある。

### 2026-09-08（後半） — 記事ページと画素からの採色で詰め直す（beta v0.12.0）

利用者の指示「中断せず続けて様々なページを再確認したりコードを取得して完全再現を目指して」。

- **画素から色を採る手法を確立**（`docs/FINDINGS.md`）。Nuxt/Next.js のサイトは配色が
  JS の中にあって静的解析では取れないが、**描画できれば画面写真の画素を読めばよい**。
  スクリーンショットを `data:` URI で `<img>` に読ませ、canvas の `getImageData` で1点ずつ採る。
  → 原神の CTA は金ではなく **`#ffcf0d`**、スターレイルの青は **`#307af7`**、
    金は **`#c9af85`**、ヘッダー帯は **`#121212`** と判明。すべて直した。
- **NTE 公式は「2つの顔」を持っていた**（`public_list260427.css` の実測）。
  **ニュース一覧＝暗い地**（`#e8e8e8` のタイトル／`#aaa` の説明／3pxの区切り／ホバーで反転）、
  **記事本文＝明るい紙に `#313131` の文字**。
  → UIモード `nte` を作り直し、**暗いテーマ＝一覧、明るいテーマ＝記事**に割り当てた。
    テーマ設定を無視せずに再現できるようになった。
  → 公式のシアン `#4fe5fb` は明るい紙で読めない（1.3:1）ので、**塗りは実測のまま・
    文字に使う側だけ `#0b6f80`（4.8:1）に落とした**。公式もシアンは暗い面だけで使っていた。
  → ページ内ナビの印（**四角 `#56e4f8` → 現在地でピンク `#fb5692` の菱形に45度回転**）を目次に入れた。
- **エンドフィールドの下層ページの型**を公式CSSから写した:
  **上白・下黄の2段ベタのヘッダー帯**＋下端に太い灰罫（`1rem solid #c6c6c6`）、
  **黒い正方形に黄色のアイコン**、**一覧の区切りは中央寄せ90%幅の細線**、
  **丸ボタンは二重リング＋ホバーで蛍光イエロー**。
- **原神のニュース一覧が描画できた**（UIパーツの初確認）。ヘッダーは暗い半透明の帯に
  白い大文字英字メニュー、右端に黄色のCTA、カードは角丸4px＋`0 3px 7px rgba(0,0,0,.35)`。
  → 「クリーム地＋白6px枠＋金の装飾」は**ホームと404の顔**で、一覧は別の顔だと分かった。両方実在する。
- **スターレイル**: ワールドページも黒地と確認。見出し帯の先頭は点ではなく**短い縦の棒**だった。
- **鳴潮**: 下層ページは描画できず（JSタイムアウト）。前回 CSS チャンク16本を実測済みのため変更なし。

### 2026-09-08 — 下層ページまで調べ直し、NTE の UIモードを新設（beta v0.12.0）

利用者の指示「もう1回様々なページを調べて再現して／一応 NTE も NTE の UI 再現して（設定から変更可能）に」。

- **UIモード `nte` を新設**（`src/styles/ui-nte.css`）。設定 → 外観 → UIモードから選べる。
  NTE 公式サイトの実測どおり **濃灰 `#1d1d1d` × シアン `#4fe5fb`**、ボタンはシアン地に濃灰の文字、
  見出しは**赤 +2px / シアン −2px の色ズレ**（公式の `.glitching` の静止版）。影は使わない（実測0件）。
- **落とし穴**: `html[data-ui='nte']`（詳細度 0,1,1）では `themes.css` の
  `:root[data-theme='minimal']`（0,2,0）に負けて `--accent` を上書きできない。
  → 変数を定義するブロックだけ **`html:root[data-ui='nte']`（0,2,1）** にした。
  配色を変えるモードを足すときは同じ書き方にすること（`docs/ARCHITECTURE.md` に明記）。
- **エンドフィールドの取り違えを2つ訂正**（公式CSS 9本・370KB を読み直した）:
  カラーバーは**2色ではなく3色**（`#ff00f0` → `#fffa00` → `#00ffa2`）、
  ハザード帯は 45度・黄色ではなく **−45度・線3px・周期6px・`#1f1f22`**。角丸も 2px → **4px**。
  ボタンのホバーで**右向き三角が右へ滑る**演出も実測どおり足した。
- **スターレイル**: キャラクターページの描画に成功（330リクエスト・JSエラー0）。
  **トップは白地だがキャラページは黒地**と判明。見出しは `・キャラクター` の形で
  **黒帯が右へフェードして消える**。パネル四隅に短い鉤。→ 見出しと角に反映。
- **原神**: `/ja/character` は 404 だったが、404 ページも公式デザインなので答え合わせになった。
  **画面のまわりの金の細枠と四隅の角飾り**、**金の細線＋四芒星の区切り**を確認 →
  「解釈」で組んでいた部分が実物と同じだと分かり、飾りを菱形から**四芒星**に直した。

### 2026-09-07 — 4つの公式サイトを実際に見てUIを作り直した（beta v0.12.0・前半の追加）

利用者の指示「4つの公式サイトを playwright で見て UI を完全再現」。

- **Playwright で外部サイトが開けるようになった**。直アクセスは切られるが、
  `ctx.route('**/*')` で全リクエストを横取りして **`curl` で取った中身を返す**と動く。
  前回「開けない」と結論づけたのは**プロキシのポートを直書きしていた**ため（誤り）。
- **エンドフィールドの配色を大きく直した**。実画面では**黒はローディング画面だけ**で、
  本編は**白地が主**だった。左レールも白地に黒アイコン。→ `endfield.css` を白地ベースに全面書き換え。
- **スターレイル**: 実画面のヘッダーは `HOME / NEWS / CHARACTER / WORLD` の大文字英字メニューで、
  **現在地は青**（黄ではない）、CTA は**金**、`border-radius: 0 31.5px 0 0`（右上だけ角丸）を11回実測。
  → `.hsr-nav` を CSS と `HsrLayout.astro` の両方に追加。
- **原神**: `border: 6px solid #fff` を7回実測（**白い6pxの太枠**が意匠の核）。
  暖色の半透明 `rgba(38,18,12,.62)`、和文は游ゴシック体。→ `.gs-card` に反映。
- **鳴潮**: 実画面は描画できなかったが、**JS バンドルから CSS チャンク16本を辿って実測**。
  アクセントは**シアンではなく金**（`#dab67d` / `#c9ac67` / `#c4ac7d`）、罫線は極細 1px、
  角丸は**左上と右下だけ**（スターレイルの右上とちょうど逆）。→ `wuwa.css` を全面書き換え。

**注意**: 圧縮された1行 CSS に `grep -o` は無言で失敗する（`grep -c color` すら 0）。
**必ず `node` で読む**。手順は [docs/UI-RESEARCH.md](./docs/UI-RESEARCH.md) の
「★ CSS の静的解析でつまずいた点」。

### 2026-09-06 — サイト全体の監査（beta v0.11.0）

5フェーズすべて完了。記録は **[docs/AUDIT-2026-09.md](./docs/AUDIT-2026-09.md)**（何を直し、
何を確認して問題なしとしたかの全リスト）。得られた知見は `docs/FINDINGS.md` へ。

**直した重い不具合**

- **ガチャの基礎S率が 1.87%（誤）→ 0.99%（正）**。ツール3種すべてで、
  「70連で73%引ける」と実際（約50%）よりかなり楽観的な数字を出していた。
  端末に保存済みの古い値も読み出し時に補正する。
- **終了したイベントが「開催中」のまま**だった（`end` が空だと開催中と判定される仕様）。
- **イベントカレンダーの帯が横あふれ**し、ページ全体が横スクロールしていた
  （`position: relative` のまま `left`/`right` を持っていた）。

**入れた仕組み**

| 追加 | 何のため |
| --- | --- |
| `checked`（最終確認日） | 「書き換えてはいないが現行版でも正しいと確認した」を正直に言うため。`updated` とは別物 |
| `pnpm test:browser` | 40ページ × 2端末を実際に開いて操作する検査。横あふれ・JSエラー・極端な入力を見る |
| `.claude/state/` | コンテキスト圧縮への備え（DECISIONS / NOW / SESSION-LOG） |
| サイドバーの絞り込み | 259件を上から探さずに済む |

**軽くした**: 1ページ 130KB → 83KB（36%減）。サイドバーが全ページで 83KB を
占めていた（259行すべてに同じアイコンの `<svg>` とインライン style）。

**残した課題**: `docs/AUDIT-2026-09.md` の末尾を参照。

### 2026-09-06 — 新UI「New」と設定ページの整理（beta v0.11.0）

- **新UI `new` を追加**（`src/styles/ui-new.css`）。既存の新UIが質感レイヤーだったのに対し、
  **`.app` のグリッドから作り替える**：パソコンはヘッダーを左の縦レールに、
  スマホは下部ナビを浮かぶドックに。モーションは scroll-driven animations
  （`@supports` で囲い、非対応では既存の JS リビールが動く）。
- 起動スクリプトの UIモード一覧も `UI_MODES` から `define:vars` で渡すようにした（配列の二重管理を解消）。
- `ThemeMenu` のポップの位置をインラインスタイルから CSS（`.theme-menu-pop`）へ移した
  （レールでは出す向きを変える必要があるため）。
- **設定ページを整理**: 上部にジャンプリンク、各セクションは折りたたみ（開閉を端末に記憶）。
  閉じていても見出しの右に現在値が出る。全部閉じると 3454px → 534px。
- **既存の不具合を修正**: パソコンでも `.drawer-toggle`（三本線）が出ていた。
  `@media` の `.drawer-toggle { display:none }` が、後方の `.btn { display:inline-flex }` に
  同じ詳細度で負けていた。全UIモードで起きていた。
- **踏んだ罠**: 描画関数の中でコンポーネントを定義したら無限ループでページが固まった。
  → トップレベルへ移動。詳細は `docs/FINDINGS.md`。

### 2026-09-06 — wikiハブ・設定の拡張・調査結果の資産化（beta v0.10.0）

- **`docs/` を新設**（ARCHITECTURE / RECIPES / CHECKS / FINDINGS）。毎セッション同じ調査を
  やり直していたので、構造の知識をリポジトリに残した。**コードを読む前にまず docs/ を見る**。
- **`/wikis/`（wiki 一覧）を新設**。`WIKI_LIST` から組み立てるので wiki を足しても触らなくてよい。
  ヘッダー・フッター・ドロワー・404・設定・α のサイド／フッターから導線を張った。
  「よく見る wiki」を設定しても**自動移動はしない**（勝手に飛ばすと戻れなくなるため）。
- **設定を 6 → 14 件に**。文字サイズ・行間・一覧の表示／並び順・よく見る wiki・
  タップの反応・タブバーを隠す・本文の長押し抑制。設定パネルは7枚のカードに整理した。
  一覧の並び替えは **CSS の `order`** で実現（JS を足していない）。
- **設定の二重管理を解消**: 起動スクリプトの配列と `KEEP_ON_CLEAR` を `PREFS` から
  自動生成・導出。足し忘れで無言で壊れる箇所が2つ減った（残るは `ICON_PATHS` のみ）。
- α 側の iOS 最適化を NTE と同等に拡張（`alpha.css` 内で完結。`ios.css` は α で読ませない）。
- **スクロール復元は何も足さなかった**。「α で壊れている」と誤診したが、原因は
  Playwright の `click()` が要素まで自動スクロールしていたこと。`goto()` で測り直すと
  NTE も α も正しく復元される。経緯は `docs/FINDINGS.md` に残した。

### 2026-09-06 — 再発報告の切り分けと安定性の底上げ（beta v0.9.3）

- 「α のタブから NTE に飛ぶ／SVG が出ない」の再報告を受けて全経路を実クリック検証したが、
  **作業ブランチの最新では再現しなかった**（α 13ページ・タブ全押し・アイコン欠落0・JSエラー0）。
- 切り分けの結果、原因は**本番 `main` に未反映**だったこと。本番 `/alpha/` は 404 で、
  v0.9.0〜v0.9.2 の 10 コミットが `main` に入っていなかった。見ていた画面が古い版だった。
- 同じ混乱を繰り返さないため、CONTEXT の冒頭に「本番に出るのは main だけ」を明記し、
  「症状から原因を引く」表を追加した。
- 安定性: `pnpm verify` で検証を1コマンドに統合し、GitHub Actions（verify.yml）で
  push ごとに同じ並びが走るようにした。`check-ui.mjs` に wiki 間のスタイル混線検査を追加。

### 2026-09-06 — iPhone最適化・α専用UI・不具合修正（beta v0.9.2）

- **日本語の強調が壊れていた**: CommonMark は閉じの `**` の直前が全角括弧・句読点だと強調と認識しない。
  77ページで `**` が生のまま出ていた。`remark-cjk-friendly` を入れて記事を書き換えずに解消。
- **α のナビから NTE へ飛ばされる**: `wikis.ts` の ALPHA ナビに `/settings/`（NTE のページ）が
  混ざっていたのが原因。除去し、混入を防ぐテストを追加した。
- **wiki 跨ぎでアイコンが消える**: NTE は View Transitions、α は独立レイアウトなので、
  跨ぐリンクを ClientRouter に横取りさせると壊れる。`data-astro-reload` を付けてフルロードにした。
- α 専用UI（`AlphaLayout.astro` + `alpha.css`）と iPhone 最適化（`ios.css`）を追加。
- `scripts/check-ui.mjs`（`pnpm test:ui`）を追加。上記3種の不具合を機械検出できる。
- 検証は Playwright（/opt/pw-browsers/chromium-1194）で iPhone 14 Pro 相当を実操作して行った。

### 2026-09-05 — 全記事の手動点検（beta v0.9.1）

- 全264記事を通読し、公式・大手wikiと突き合わせて点検。魔の書の弱点（ニューホランドの表だけ光→闇）、
  白蔵/ファルディーヤの所属表記、潯の英語版バナー名、CV欠落5件などを修正。
- ガイド類に残っていた英語露出（Annulith / Beyond the Rails / Scarborough Fair / Fons）を日本語化。
- `scripts/check-content.mjs`（`pnpm test:content`）を追加し、記事の体裁と誠実性ルールを機械検査できるようにした。
- システム側も整理: Astro 6 で非推奨の `markdown.rehypePlugins` を `unified()`（@astrojs/markdown-remark）へ移行し、
  ビルド時の deprecation 警告を解消。未使用変数を削除し、`test/wikis.test.ts` で wiki 分離の不変条件を検査するようにした（全43テスト）。
- **残**: events / vehicles の一部は Ver.1.3 の新要素（新車3台の個別ページなど）が未収録。出典が取れ次第追加する。

### 2026-09-05 — Ver.1.3 対応とマルチwiki基盤（beta v0.9.0）

- Ver.1.3 の新キャラ・新エリア・第6章・夏イベント・新モードを追加。バージョン履歴ページを新設。
- カオスを実装済みデータへ修正、ティア表を9月時点へ同期、レクイエム弧盤PUのURLを是正（旧URLはリダイレクト）。
- マルチwiki基盤（`src/lib/wikis.ts`）と αテストwiki（`/alpha/`）を新設。

---

## 6. 作業のやり方

```bash
pnpm install        # 依存インストール
pnpm dev            # 開発サーバ
pnpm test           # vitest（31テスト）
pnpm build          # 本番ビルド（dist/ に出力）
pnpm test:content   # 記事の品質検査（出典・更新日・リンク記法・重複。ビルド不要）
pnpm test:ui        # UI検査（アイコン参照切れ・wiki跨ぎリンク・ナビ混入。build の後）
node scripts/check-links.mjs   # 内部リンク切れ検査（build の後に実行）
```

上記をまとめて実行するなら **`pnpm verify`**（test → 記事検査 → 型 → build → UI検査 → リンク検査）。
GitHub Actions（`.github/workflows/verify.yml`）でも push のたびに同じ並びが走る。

**変更したら必ず `pnpm verify` を通してからコミットする。**

コミット後は `git push -u origin claude/claude-nte-audit-E1OnP`。

---

## 7. 更新履歴（このファイル自体の）

- 2026-09-06: `docs/` を新設し、構造の詳細をそちらへ分離（v0.10.0）。このファイルは
  「今どうなっているか・何をしたか」に専念する役割に整理した。
- 2026-09-06: 本番反映の経路（main のみ）と症状対応表を追記。検証を `pnpm verify` に統合（v0.9.3）。
- 2026-09-06: α専用UI・iPhone最適化・不具合修正（v0.9.2）を反映。検証手順に `pnpm test:ui` を追加。
- 2026-09-05: 作業ログ節（第5節）を追加。区切りごとに追記する運用に変更。
- 2026-09-05: 新規作成。マルチwiki基盤の追加、Ver.1.3 対応、記事の誤り修正と同時に整備。
